<%= form_for(current_user, url: user_relationships_path(@user), method: :delete, remote: true) do |f| %>
<%= button_tag(class:"btn btn-danger rounded-pill btn-sm") do %>
<%= content_tag :span, "フォロー中", class:"fas fa-user-minus" %>
<% end %>
<% end %>